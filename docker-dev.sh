# Start Docker
docker-compose start dev postgres pgadmin

# build and start docker
# docker-compose up dev postgres pgadmin

# Build Docker and update packages
# docker-compose up --build -V dev postgres pgadmin